# 開発メモ

**このリポジトリで作業するうえで、忘れると手戻りする事実だけ**を置く。
使い方は [README.md](README.md)、利用条件は [TERMS.md](TERMS.md) を参照。

---

## 1. 実行環境

セットアップ手順そのものは [README.md](README.md) にある。ここには**踏んだ穴だけ**を残す。

起動 (**`--refs` は不要**。`refs/` を同梱してあるので引数なしで拾う):

```
python app.py                      # モデルは ./model (setup_model.py の既定)
QWEN_TTS_MODEL=<snapshot パス> python app.py   # 別の場所に置いている場合
```

> **開発機固有のパス (venv・モデル・HF キャッシュの実際の置き場所) はここに書かない。**
> 公開リポジトリなので、他人には意味が無く、自分の環境も晒すことになる。
> 手元の値は gitignore してある `DEVNOTES.local.md` に置く。

- **uv で作った venv には `pip` が入っていない。**追加導入は
  `uv pip install --python <venv>/Scripts/python.exe <pkg>`

- **`--model` に HF の repo id を渡すと数分ハングする。**必ずローカルパス。
  切り分けは `HF_HUB_OFFLINE=1` (ハングせず即エラー)
- **VRAM 8GB では余裕が無い** (モデル 3.4GB + 32 秒生成の KV キャッシュで上限)。
  「冒頭は正常」経路は生成が 2 回走るので、1 回目のキャッシュを抱えたままにしない
- **初回生成はウォームアップで RTF 22。実力値ではない** (定常は 4〜5)。
  これを実力と誤認して所要時間を外挿する事故を実際に起こした
- **量子化は効かない** (ディスパッチ律速。重み読み出しは 385ms 中の 7.6ms)

## 2. 音割れ — 原因は特定済み、後処理では直らない

**クリップはこちらのコードではなくモデル内部で起きている。**
`qwen_tts/core/tokenizer_12hz/modeling_qwen3_tts_tokenizer_v2.py:884` の
ボコーダ最終行が `return wav.clamp(min=-1, max=1)`。
**超過分はこちらが受け取る前に潰されている**ので、出力側にピーク正規化を掛けても無意味
(頭が平らになった波形を小さくするだけ)。

**退けた仮説** (どちらも実測で否定。同じ道を辿らないこと):

- 「最終出力を正規化していないから」 → 上記のとおりモデル内でクランプ済み
- 「線形クロスフェードで振幅が持ち上がる」 → **逆。**線形 `a*(1-r)+b*r` の出力は常に
  両者の最大値以下に収まる (無相関なら -3dB 沈む)。**等パワーに変えると 1.41 倍まで
  持ち上がるので変えてはいけない**

実測 (31.94s / 冒頭 3〜4 秒は正常 + 以降 `ramp_reverse`):

| 区間 | peak | RMS | >0.99 の率 | スペクトル平坦度 | 6kHz 以上 |
|---|---|---|---|---|---|
| 正常 (0-3.5s) | 0.401 | 0.0535 | **0.000%** | 0.009 | 0.30% |
| 崩壊 (4.5s-) | 1.000 | 0.1222 | 0.083% | 0.178 | **12.45%** |

**正常部は割れない。割れるのは崩壊部だけ。**崩壊部は正常部より RMS で +7.2dB 大きく、
6kHz 以上のエネルギーが 40 倍・平坦度が 19 倍 (これはエフェクトの性質でバグではない)。
根本策は「モデルにクランプへ振り切らせない」の一点で、レバーは参照レベルと崩壊の強さ。
**参照レベルが出力レベルを支配するかは未検証。**

## 3. 参照音声の作り方 (無音バグの教訓)

- **端の無音は必ず落とす。**ICL は参照の音響特性をそのまま写すので、無音が残ると
  出力の読点が 0.4〜0.9 秒に伸びて「途中で音が途切れた」ように聞こえる。
  修正前は参照の 50〜67% が無音だった
- **連結時に無音を挟まない。**20ms クロスフェードで繋ぐ
- **`snr_db` は端の無音を落としてから測る。**落とさないと
  「無音の多いクリップほど高 SNR」という逆の順位付けになる
- `ref_text` は必須。ただし**モデルが見ているのは音声との一致ではなく「日本語として
  自然か」**なので、多少の転記ズレは実害が小さい。逆に文として壊れた文字列 (逆順など)
  を渡すと生成そのものが崩壊する

## 4. 崩壊のしくみ

- **崩壊は参照音声を壊すことで起こす。**サンプリング温度をいくら上げても
  (8.0 まで確認) 崩れない
- 採用レシピは `ramp_reverse` (後半へ行くほど逆再生する)。静的に壊した参照
  (全体を逆再生など) はモデルが「壊れた状態」で定常化して途中から進行が止まる
- **崩壊は確率的で、参照が長いほど確実**(実測: 11.5 秒で 3/3、3.8 秒で 2/3、5.0 秒で 1/3)。
  シードを固定すれば同じ結果がビット単位で再現される
- **ただし「長い参照を選べば当たる」ではない。**noa は 15.9 秒の set6 で
  **シード 3 通り × 2 段すべて不発**、11.5 秒の set1 で 1 発目から崩壊した。
  **文にも強く依存する** — 同じ set6 で台詞系の文は全滅、地の文は set1 で即崩壊。
  当たり外れは `make_samples.py --probe` で探すこと (§10)
  (この set1〜6 は **2026-08-17 に 8 スタイルへ差し替えた旧参照**。
  観測そのものは有効だが、同じ名前の参照はもう無い)
- **キャラによっては当たらない。**8 キャラ × 地の文 × シード 1,7 の実測 (`--probe`):

  | | 崩壊前夜 | 完全崩壊 |
  |---|---|---|
  | 文月 乃亜 (set1) / 御影 怜香 / 御影 すず / 沢渡 ゆめ | ○ | ○ |
  | 御影 司 / 久遠 霞 | ○ (シード次第) | **×** |
  | 柊 律 | **×** | ○ |
  | プリヤ・シャルマ | **×** | **×** |

- **「完全崩壊」のほうが崩壊しにくい** (上の表の司・霞)。`chunk_sec` が 0.50 → 0.25 と
  細かくなるためで、**細かく刻むと崩壊しなくなる**という既知の性質
  (実測 0.10/0.40/1.0 で 0/2) と整合する。**いちばん強い段がいちばん当たりにくい**
  という逆転が起きている。既定値は音質とのトレードオフで選んだ点なので動かしていない
- **崩壊したかの機械判定は「上限張り付き + 崩壊部の書き起こしが意味不明」。**
  指標 (高域比 / 平坦度) では判定できない: 完全崩壊は `warp` で速度が落ちるので
  **高域比と平坦度が下がる**方向に動き、素の生成と見分けが付かない。
  **書き起こしを音声全体に掛けても駄目** — 頭は正常な推論なので、そこの正しい文が
  出てきて一致率が上がり、後ろが壊れていても見えない。**頭と後ろを分けて見る**
- 任意入力への転用は 6/9 で「まだら」。**完全な入力非依存ではない**
- `min_new_tokens` は 2 でハードコード、`eos_token_id` は無視される
  (実際の停止判定は `codec_eos_token_id`)。強制継続はそこを触る
- `max_new_tokens ÷ 12.5 = 秒数`。上限があるので無限ループにはならない

## 5. キャラと参照の対応

`CHARACTER_NAMES` (`app.py`) が**許可リスト**。ここに無いキャラの参照は
`load_refs` が読み込まない。**除外リストは持たない** — 出さないと決めた
キャラの名前がコードに残ってしまうため。

乃亜の参照は novtube 側 (`tool/tts/style_refs/style_sources.yaml`) が SoT。
2026-08-18 に向こうで `cool` → `whisper`「ささやき」へ改名し、`cool2` と `emotional` は
不採用になった。**不採用の 2 つはこちらには残す** — 崩壊の素材は多いほうがよいため。
`hoge` は冒頭・末尾の孤立短語を除いて 10.9s → 8.8s に短くなっている
(短語で始まると Qwen が出だしの癖ごと写し、生成音の頭に異物が乗る)。

| ID | 表記 | 参照 (実発話長) |
|---|---|---|
| `noa` | 文月 乃亜 | normal(10.5s) normal2(13.0s) whisper(13.6s) boyish(9.6s) hoge(8.8s) lazy(9.7s) / 本家不採用: cool2(11.8s) emotional(10.8s) |
| `tsukasa` | 御影 司 | normal_01(13.4s) normal_02(12.0s) |
| `ritsu` | 柊 律 | normal_01(15.0s) normal_02(14.8s) |
| `reika` | 御影 怜香 | normal_01(14.6s) normal_02(14.7s) |
| `suzu` | 御影 すず | normal_01(13.1s) normal_02(10.6s) |
| `priya` | プリヤ・シャルマ | normal_01(10.2s) normal_02(12.9s) |
| `yume` | 沢渡 ゆめ | normal_01(11.0s) normal_02(10.0s) |
| `kasumi` | 久遠 霞 | normal_01(13.3s) normal_02(10.2s) |

- `refs.json` の `wav` は**そのファイルからの相対パス**。
  絶対パスを書くと作った機械のパスが埋まる
- **同梱できるのは Common Voice (CC0) 由来のものだけ。**出所が別のキャラは入れない
  (再配布条件が違う)。→ [CREDITS.md](CREDITS.md)

## 6. UI の決まりごと

- **タイルの絵は崩壊度に連動して 3 段階**。判定は `collapse_fx.tile_state()` の 1 箇所で、
  かんたん設定と詳細設定が同じ軸 (`start`) を通る。モードごとのしきい値を持たない

  | `start` | 絵 |
  |---|---|
  | > 0.75 | `assets/chibi/` |
  | 0.375〜0.75 | `assets/chibi_collapse/` |
  | ≤ 0.375 | `assets/chibi_gore/` (**モザイク焼き込み済**) |

  その段の絵を持たないキャラは 1 段手前へ落とす (欠けたタイルを出さない)
- **タイル URL には `?v=<素材の最終更新時刻>` を付ける。**付けないと画像を作り直しても
  ブラウザがキャッシュから古い絵を出し続け、「差し替えたのに変わらない」になる (実際に起きた)
- **「あなた専用キャラ」は書き起こしが揃うまで生成ボタンを押させない**
  (`gen_blocked`)。理由を文字でもボタン横に出す。ASR が失敗しても `finally` で
  フラグを下ろすので手入力に切り替えられる
- **モザイク前の gore 画像は配布物に入れない** (`assets/*_candidates/` は gitignore)。
  CSS で隠すと DevTools で外せてしまうため、モザイクは焼き込む
- **なれの果て (`chibi_gore`) は既定では「人が崩れた残骸」を順に配るだけ**
  (`GORE_VARIANTS` を index で割り当て)。**人でないキャラには当たらない**ので、
  その場合は `GORE_PROMPTS[<id>]` にプロンプトごと書く
  (mossan_hoshi は煎餅で、素のタイルもイラストではなく写真。被写体も絵柄も
  まるごと差し替えて「砕けた煎餅くずの山」の写真にしている)

## 7. ロゴ

- **フォントの絵文字を画像として持ち込まないこと。**ブラウザで表示した 🔞 を
  スクショして焼き込んでいた時期があり、その絵柄はフォントベンダー
  (Segoe UI Emoji / Noto など) の著作物だった。**絵文字は「書体」ではなく「絵」**として
  保護されうるので、all rights reserved と宣言する自社ロゴに混ぜてはいけない
- 描き直しは `python make_logo.py --redraw-emoji` (**ロゴ 1 枚だけを渡す**。
  絵文字画像は渡さない)
- **既知の未修正**: ロゴの「崩壊」の字形が崩れている。直すならロゴ全体の再生成
  (`make_logo.py --variants N`)
- `fit_logo()` は**暗背景前提**で書かれている。白背景で返ってきた生成物を通すと
  何も抜けずに白い正方形になる

## 8. ライセンスの立て方

- **コードのみ** Apache-2.0 ＋ [TERMS.md](TERMS.md) の追加条件
- **キャラ画像とロゴは carve-out** (all rights reserved)。Apache-2.0 を全体に掛けると
  ブランド資産まで誰でも再配布・改変できてしまう
- `refs/*.wav` は **CC0 1.0 ＋「話者の特定を試みない」**(Common Voice 利用規約)。
  **CC0 が及ぶのは録音の著作権だけで、話者の人格権・パブリシティ権は別**
- 利用目的に制限があるので **OSI 定義の「オープンソース」ではない。**
  単に「Apache-2.0」と呼ばない

## 9. 由来

**「のべつべ！」の非公開リポジトリにあった内製ツールを切り出したもの。**
**git 履歴は持ってきていない** (このツールから外したキャラの痕跡が残るため、
現ツリーだけをコピーして新規 `git init` した)。

Qwen3-TTS への移行そのものに関する調査結果 (誤読率、SFT の結論、蒸留案など) は
**元リポジトリ側のドキュメントが正典**で、こちらには持ってきていない。

---

## 10. README のサンプル音声表

生成は `make_samples.py` (**アプリは立ち上げないが、通る経路は `app.synthesize` と同じ**)。
表の markdown も同スクリプトが `samples/table.md` に吐き、README の
`<!-- SAMPLES:BEGIN -->` 〜 `END` の間に貼る。

- **UI の既定値をスクリプトの定数に写してある** (`DUR_SEC` / `HEAD_LO,HI` / 温度)。
  表は「素の既定で出る音」でなければ意味が無いので、ここを触ると表と実物がずれる
- **`<audio>` は GitHub がサニタイズする**ので README にそのまま書いても消える。
  **mp3 単体の blob ページにも GitHub 標準の再生プレイヤーは付かない** (実測。
  「Raw」リンクとダウンロードボタンのみ) — サムネ画像をファイルへのリンクにする案は
  クリックしてもページ遷移するだけで鳴らせないので採らなかった
- **その場で鳴らす方法は `user-attachments` 埋め込みしかない。**音声を無音動画
  (静止画+音声の mp4) に変換し、GitHub の Web エディタへドラッグ&ドロップすると
  `https://github.com/user-attachments/assets/<hash>` という URL が発行される。
  この URL を `<video src="...">` で参照するとページ内蔵のプレイヤーが描画される。

  ### 書き方は 3 通り試して 1 つしか使えない (**ここを間違えて README を 2 回壊した**)

  | 書き方 | 結果 |
  |---|---|
  | bare URL を裸で置く | ○ 自動でプレイヤー化。ただし**前後を空行で挟んだブロックレベルの段落限定**。表のセルではただのリンク |
  | `<details>` + 空行 + bare URL | **× セル内の空行が markdown の表を分断する。**表が崩壊し動画が表の外へはみ出す |
  | **`<video src="...">` を直接書く** | **◎ これが正解。**GitHub はサニタイズしない。空行不要で 1 セル = 1 行に収まり表を壊さない。折りたたみも要らず最初からプレイヤーが見える |
  | `<video src="<repo>/raw/<branch>/....mp4">` | **× セルごと空になる。**リポジトリ内の mp4 を raw URL で指す形はサニタイザが丸ごと落とす (実測: 描画後の `<td>` が空)。**動画の src に使えるのは `user-attachments` だけ** |

  → `player_cell()` は 3 番目。**セルの中に空行を作らないこと**が絶対条件。
  リポジトリに mp4 をコミットして raw で参照する「手動アップロード不要」案は
  4 番目が潰れたので**成立しない**。

  - **この URL は private リポジトリでも「知っていれば誰でもアクセスできる」。**
    リポジトリの権限とは無関係。同梱音声 (Common Voice の匿名話者 / mossan_hoshi の声)
    をこの形で公開することの是非はユーザーに確認済みで、了承のうえで採用した
  - **発行は Web エディタへの手動アップロードでしかできない** (`gh` の REST API では
    発行できない、ブラウザのセッション認証が要る内部エンドポイントのため)。
    ブラウザ拡張を繋いで `file_upload` → 捨ててよい scratch ファイルの編集画面
    (`/new/main?filename=_scratch_x.md`) でアップロードし、返ってきた URL を読み取る。
    **scratch ファイルはコミットしない** (URL は発行済みなのでページを離れれば済む)
  - **ファイル入力は `multiple`。**34 本を 1 本ずつ投げる必要はなく、
    `input#blob-dragged-file-input` へまとめて渡せる (`file_upload` の 10MB 上限に
    合わせて 3 回に分ければ足りる)。挿入順は保証されないが、下の対応取りが順序に
    依存しないので問題にならない
  - **URL とファイル名の対応は「Preview を描画させて DOM から読む」のが正解。**
    エディタに挿入されるのは裸の URL だけでファイル名が無いが、**Preview は各動画を
    `<details><summary>ファイル名</summary><video>` で包む**ので、
    `summary` のテキストと `video` の src に埋まった UUID を突き合わせれば
    34 組が 1 回で正確に取れる。**挿入順を数える必要も、スクロールして読む必要もない**
    (エディタ本体は CodeMirror の仮想スクロールで全文が読めないので、
    そちらから回収しようとすると詰まる)
  - **GitHub は全ての埋め込み動画に `muted` 属性を強制付与する。これは markdown 側から
    解除できない** — **`POST /markdown` の出力で確認済み**で、サーバ側のサニタイザが
    `<video>` を固定形に**書き換えて**いる (こちらが書いた属性は捨てられる)。
    以下 7 通りを試して**出力は全て同一**だった:
    bare URL / `<video src>` / `controls` 付き / `muted="false"` 明示 / `autoplay` /
    `<video><source></video>` / `<p>` 包み。`<audio>` は要素ごと削除される。
    書き方 (bare URL / `<video>` タグ / 表のセル) に関係なく、結果は必ず
    `<video controls="controls" muted="muted" style="max-height:640px; min-height: 200px">`。
    **「別の書き方なら鳴るのでは」を試し直さないこと** (7 通り潰してある)。
    **つまり視聴者は ▶ を押しただけでは音が出ず、🔊 を押す必要がある。**
    プレイヤー側では解除できない (`muted` を外す手段が無い) ので、
    **README に注意書きを出すのが唯一の対処** (`make_samples.MUTE_NOTE`)。
    これを知らずに「崩壊段だけ音が出ない」を**ファイルの不具合だと誤診した** —
    実際には全段ミュートで、たまたま通常段だけ 🔊 を押していただけだった
  - **表示サイズは `min-height: 200px` で下から効く。**`<video width=...>` は
    GitHub 側で無効化される (実測)。動画の解像度が 120x120 でも表示は約 200x200 に
    引き伸ばされ、横幅はセル幅で決まる。**つまり列を減らすとプレイヤーが大きくなる**
    (読み上げテキスト列を落としたのはこの理由もある)。480x480 で作ったときは
    「デカすぎる」と指摘された。**サイズを変えるたびに 34 本の作り直し・再アップロード・
    `embed_urls.json` の総入れ替えが要る**ので、先に 1 本で見え方を確かめること
  - **映像と音声の長さを必ず揃える** (`make_mp4` が `-t` で明示)。
    `-loop 1` の静止画に `-shortest` を付けても**効かず、映像が音声より 14 秒長い**
    mp4 が出来ていた (実測: 映像 33.5s / 音声 19.4s)。**これは音が出ない原因ではない**
    (上記のとおり真因は `muted`) が、再生し終わったあと十数秒ぶん無音の映像が続き
    タイムラインが実際より長く見えるので直した。作ったら
    `ffprobe -show_entries stream=codec_type,duration` で**両ストリームの長さが
    一致することを確認する**
  - mp4 生成は `make_embeds.py` (`samples/_embeds/` に出力。**リポジトリには入れない**
    — 配布物としては mp3 で足りる)。URL の対応は `samples/embed_urls.json`
    (`<char>_<ref|normal|eve|full>` → URL)。**この対応はコード側では再現できない**
    (URL は毎回新しく発行される) ので、キャラを増やす/差し替えるたびに手順を繰り返す
- **表の見出しの列数と、実際に作るセルの数を必ず一致させる。**「シード値」という見出し
  だけあってセルを作っていないバグで**表全体が崩壊した**。シードは列を割らず、
  各結果セルの中に `<sub>` で添えている
- 音声は **mp3**。24kHz mono の wav は 1 秒 48KB で、3 段 × 8 キャラだとリポジトリが
  数十 MB 太る
- **崩壊は確率的なので、崩れなかった行はシードを変えて引き直す**。
  探索は `--probe 1,7,13`(シード列) `--ref` / `--preset`(組み合わせ替え) で行い、
  **当たりを `SAMPLES` に書き戻して固定する**
- **探索も本番と同じ「冒頭は正常」付きで作る。**崩壊するかは頭の有無で変わらないので
  切れば探索は倍速になる — が、**耳で選ぶ相手が本番と違う形をしているのは駄目**で、
  実際に「冒頭から意味不明」と混乱を招いた
- **全行で同じ文を読ませている** (`SAMPLE_PRESET` = 地の文)。文によって崩壊の当たり方が
  変わるうえ、同じ文なら声そのものを比べられる
- 崩壊の当たり判定は `--verify` (頭と崩壊部を分けて書き起こす。§4 参照)
- **表は 4 列 = 参照音声 / 通常 / 崩壊前夜 / 完全崩壊。1 行 = 1 キャラ。**
  崩壊が当たらなかった段はセルが `—` になる。
  (「崩壊度を 1 列にして当たった段だけ行を並べる」案も作ったが、
  **表の形が崩壊度によって変わる**ので 3 列固定に戻した)
- **読み上げテキストは全行同じなら列ごと落として表の外に 1 回だけ書く**
  (`table()` が `set(texts)` の要素数で自動判定。行ごとに違う文を混ぜたときだけ
  列が復活し、直前の行と同じなら〃になる)。同じ文を 9 回並べても情報が無いうえ、
  **横幅を食ってプレイヤーが小さくなる** — プレイヤーの表示幅はセル幅で決まるため。
  シードは `<sub>` の小さい文字で結果セルの中に添える — 主役は音声
- **README への差し込みは `patch_readme()` が自動でやる** (`SAMPLES:BEGIN/END` 間を
  置換)。**手でコピペしない** — 貼り損ねと列ずれで表を 2 回壊している

## やること

### 既知の未修正

- ロゴの「崩壊」の字形 (§7)
- 音割れ (§2。根本策は参照レベルの検証が前提)
- PyInstaller のビルドは**未実施**
